# pillApp — הנחיות לקלוד

אפליקציית PWA סטטית לניהול תרופות. עברית, RTL, מותאמת למשתמש/ת מבוגר/ת.

## כללי ברזל בפרויקט הזה

1. **אין שרת ואין שלב בנייה.** הכול קבצים סטטיים + ES modules שנטענים ישירות.
   אל תוסיף bundler, framework, או npm — זה נועד לרוץ מכל אחסון סטטי ומכל תיקייה.
2. **המפתח של Gemini לעולם לא נכנס לקוד או ל-repo.** הוא נשמר ב-localStorage בלבד,
   והמשתמש/ת מדביק/ה אותו פעם אחת בהגדרות. אם צריך מפתח לבדיקה מקומית — דרך `bws run` בלבד.
3. **בכל שינוי קוד — לעדכן את חותמת הגרסה בשני מקומות:**
   `js/store.js` (`export const BUILD`) ו-`sw.js` (`const BUILD`, גם קובע את שם ה-cache).
   הפורמט: `YYYY-MM-DD HH:MM vN label`. החותמת מוצגת בתחתית כל מסך.
   בלי עדכון ב-`sw.js` הדפדפן יגיש קוד ישן מהמטמון.
4. **טיפוגרפיה גדולה זה פיצ'ר, לא באג.** שם התרופה והכמות בתזכורת חייבים להישאר ענקיים.
   כל מטרת מגע לפחות 56px (`--tap`). לא לצמצם.
5. **כל טקסט חדש חייב לכבד מגדר.** להשתמש ב-`g('נקבה','זכר')` מ-`js/text.js`, לא במחרוזות קשיחות.

## מבנה

| קובץ | אחריות |
|---|---|
| `js/store.js` | סכמת הנתונים, localStorage, עזרי תאריך, CRUD, BUILD |
| `js/schedule.js` | האם תרופה נלקחת בתאריך, מנות היום, איחורים, מלאי, היענות |
| `js/text.js` | כל הניסוח העברי האישי |
| `js/notify.js` | לולאת בדיקה כל 15 שניות, נדנוד, נודניק, קול, יום שקט |
| `js/sensors.js` | DeviceMotion (השכמה), Geolocation (יציאה מהבית) |
| `js/tools.js` | מצלמה משותפת: פנס + זכוכית מגדלת + דחיסת תמונות |
| `js/gemini.js` | קריאות ל-Gemini: זיהוי מצילום (responseSchema), מידע (google_search) |
| `js/dom.js` | `$`, `el`, טוסטים, גיליון תחתון, `confirmBig`/`promptBig` |
| `js/ui.js` | חמשת המסכים, התזכורת הגדולה, זכוכית מגדלת, מסך שבת |
| `js/editors.js` | עורך תרופה, כרטיס מידע, עורך פרוצדורה |

## נקודות עדינות שכבר נפתרו — לא לשבור

- **`slotsForDate` מסננת מנות שקדמו ל-`med.createdAt`.** בלי זה, תרופה שנוספה היום
  מייצרת עשרות "החמצות" מדומות למפרע ומרסקת את אחוז ההיענות.
- **`confirmBig`/`promptBig` פותחים שכבה עצמאית**, לא `openSheet`. אחרת ביטול
  בתוך עורך התרופה סוגר את העורך ומוחק את מה שהוקלד.
- **`doseTick` יוצאת מוקדם אם התזכורת הגדולה כבר פתוחה**, ומתריעה על מנה אחת בכל פעם.
- **`quietDayTick` צורכת מכסת-הודעה רק אם יש באמת מה להכריז.**
- **`.reminder-body` ממורכזת דרך `margin:auto` על הילד הראשון והאחרון**, לא
  `justify-content:center` — אחרת תוכן גבוה מהמסך נחתך מלמעלה ולא ניתן לגלילה.
- **מאגר משרד הבריאות חסום ב-WAF.** אל תנסה לגשת אליו ישירות או דרך פרוקסי;
  זה נבדק ונכשל. המידע מגיע מ-Gemini עם חיפוש מקורקע.

## בדיקה

`start.bat` מרים שרת ב-8899. לבדיקה בדפדפן, `window.pillApp` חושף את `S`, `UI`, `N`, `Sensors`, `Sch`
כדי לקרוא ל-`pillApp.UI.showView('track')` וכדומה בלי לצוד קואורדינטות.
