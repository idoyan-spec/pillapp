# pillApp — הנחיות לקלוד

אפליקציית PWA סטטית לניהול תרופות. עברית, RTL, מותאמת למשתמש/ת מבוגר/ת.

## כללי ברזל בפרויקט הזה

1. **אין שרת ואין שלב בנייה.** הכול קבצים סטטיים + ES modules שנטענים ישירות.
   אל תוסיף bundler, framework, או npm — זה נועד לרוץ מכל אחסון סטטי ומכל תיקייה.
2. **המפתח של Gemini לעולם לא נכנס לקוד או ל-repo.** הוא נשמר ב-localStorage בלבד,
   והמשתמש/ת מדביק/ה אותו פעם אחת בהגדרות. אם צריך מפתח לבדיקה מקומית — דרך `bws run` בלבד.
3. **בכל שינוי קוד — לעדכן את חותמת הגרסה בשני מקומות:**
   `js/store.js` (`export const BUILD`) ו-`sw.js` (`const BUILD`, גם קובע את שם ה-cache).
   הפורמט: `YYYY-MM-DD HH:MM vN label`. החותמת מוצגת בתחתית כל מסך.
   בלי עדכון ב-`sw.js` הדפדפן יגיש קוד ישן מהמטמון.
4. **טיפוגרפיה גדולה זה פיצ'ר, לא באג.** שם התרופה והכמות בתזכורת חייבים להישאר ענקיים.
   כל מטרת מגע לפחות 56px (`--tap`). לא לצמצם.
5. **כל טקסט חדש חייב לכבד מגדר.** להשתמש ב-`g('נקבה','זכר')` מ-`js/text.js`, לא במחרוזות קשיחות.
6. **זיהוי לפי תמונת כדור בלבד לעולם אינו ממלא שדות מעצמו.** ראה "כלל הבטיחות" למטה.
   זה לא סגנון — זו הגנה מפני מילוי שם תרופה שגוי ברשימת התרופות של אדם.

## כלל הבטיחות — זיהוי מתמונה

נמדד בפועל מול ה-API (לא הנחה): **קריאת האריזה אמינה; זיהוי לפי כדור בלבד אינו אמין.**
בבדיקה עם חריטה `TEVA 3109`, שלב החילוץ הוויזואלי קרא אותה מושלם, אבל שלב הזיהוי החזיר
`identified:false`, `matchConfidence:'low'`, **אפס מקורות חיפוש**, והזהיר בעצמו שייתכן שמדובר
בקרבמזפין ולא באמוקסיצילין. אותה חריטה יכולה להופיע על תרופות שונות, וצורה+צבע אינם מזהים.

לכן `applyIdentification()` ב-`editors.js` מפצל:

| מקור | התנהגות |
|---|---|
| יש שם מהאריזה (`v.name`/`nameEnglish`/`genericName`) | `fill()` רץ אוטומטית — מילוי אגרסיבי |
| כדור בלבד, `identified:true` + `matchConfidence:'high'` | `fill()` רץ אוטומטית |
| כדור בלבד, כל מצב אחר | **לא ממלא כלום.** פאנל כתום + הסבר |
| יש `mismatchWarning` או `matchConfidence:'low'` | **גם כפתור "מלא בכל זאת" לא מוצג** |

`mismatchWarning` הוא בדיקת בטיחות אמיתית: כששולחים גם אריזה וגם כדור, המודל מתבקש לבדוק אם
הכדור תואם למה שכתוב על האריזה — כדור שגוי באריזה הוא תרחיש קיים.

**תמונת הכדור שווה גם בלי זיהוי:** היא מוצגת גדול בתזכורת, ו-`pillDescription()` מציג
"לבן · עגול · M 50". זה מה שמונע בפועל בלבול בין כדורים דומים — יותר מכל זיהוי אוטומטי.

## מלכודת המטמון — נשרפנו בה, אל תחזור עליה

הדפדפן הריץ `text.js` ישן בזמן שהשרת הגיש חדש, והתוצאה הייתה `TypeError` שהפיל את
התזכורת הקופצת — עם מספר גרסה חדש שהראה שהכול תקין. שלוש ההגנות שנוספו, אל תסיר אותן:

1. `sw.js` install מוסיף כל קובץ עם `new Request(u, {cache:'reload'})` — בלי זה המטמון
   החדש יכול להתמלא בקבצים ישנים ממטמון ה-HTTP.
2. הרישום ב-`app.js` הוא `{updateViaCache:'none'}` — אחרת `sw.js` עצמו נמשך ממטמון.
3. `controllerchange` גורם ל-`location.reload()` חד-פעמי, כדי שלא ירוץ ערבוב של קוד ישן וחדש.

בבדיקה מקומית תמיד לנקות: unregister ל-SW + מחיקת כל ה-caches, אחרת בודקים קוד ישן.

## מנוי דחיפה שפג — אל תחזור על הטעות

FCM החזיר `410 Gone` על מנוי שפג (קרה כנראה אחרי התקנה למסך הבית).
הגרסה הקודמת **מחקה את הרשומה בשקט**, האפליקציה לא ידעה, והממשק המשיך
להציג "✅ פעיל" בזמן ששום תזכורת לא יכלה להישלח. תקלה שקטה מהסוג הגרוע.

ההגנות שנוספו — אל תסיר אותן:
1. השרת **מסמן** `dead` במקום למחוק, עם סיבה וזמן.
2. `GET /api/status?id=` חושף `exists/dead/nextSlot/lastSentAt`.
3. `Push.verify(true)` רץ בכל פתיחה ומרפא לבד.
4. ריפוי של מנוי מת חייב `unsubscribe()` ואז `subscribe()` — רישום מחדש
   של אותו endpoint יחזיר 410 שוב.
5. `pushsubscriptionchange` ב-`sw.js` מחדש מנוי שהדפדפן ביטל.
6. `sync()` מכבה את `enabled` אם אין מנוי — עדיף להציג "כבוי" מאשר לשקר.

**כלל:** אסור שהממשק יציג "פעיל" על סמך דגל מקומי בלבד. המקור היחיד
לאמת הוא תשובת השרת.

## מבנה

| קובץ | אחריות |
|---|---|
| `js/store.js` | סכמת הנתונים, localStorage, עזרי תאריך, CRUD, BUILD |
| `js/schedule.js` | האם תרופה נלקחת בתאריך, מנות היום, איחורים, מלאי, היענות |
| `js/text.js` | כל הניסוח העברי האישי |
| `js/notify.js` | לולאת בדיקה כל 15 שניות, נדנוד, נודניק, קול, יום שקט |
| `js/sensors.js` | DeviceMotion (השכמה), Geolocation (יציאה מהבית) |
| `js/tools.js` | מצלמה משותפת: פנס + זכוכית מגדלת + דחיסת תמונות |
| `js/gemini.js` | קריאות ל-Gemini: זיהוי מצילום (responseSchema), מידע (google_search) |
| `js/dom.js` | `$`, `el`, טוסטים, גיליון תחתון, `confirmBig`/`promptBig` |
| `js/ui.js` | חמשת המסכים, התזכורת הגדולה, זכוכית מגדלת, מסך שבת |
| `js/editors.js` | עורך תרופה, כרטיס מידע, עורך פרוצדורה |

## נקודות עדינות שכבר נפתרו — לא לשבור

- **`slotsForDate` מסננת מנות שקדמו ל-`med.createdAt`.** בלי זה, תרופה שנוספה היום
  מייצרת עשרות "החמצות" מדומות למפרע ומרסקת את אחוז ההיענות.
- **`confirmBig`/`promptBig` פותחים שכבה עצמאית**, לא `openSheet`. אחרת ביטול
  בתוך עורך התרופה סוגר את העורך ומוחק את מה שהוקלד.
- **`doseTick` יוצאת מוקדם אם התזכורת הגדולה כבר פתוחה**, ומתריעה על מנה אחת בכל פעם.
- **`quietDayTick` צורכת מכסת-הודעה רק אם יש באמת מה להכריז.**
- **`.reminder-body` ממורכזת דרך `margin:auto` על הילד הראשון והאחרון**, לא
  `justify-content:center` — אחרת תוכן גבוה מהמסך נחתך מלמעלה ולא ניתן לגלילה.
- **מאגר משרד הבריאות חסום ב-WAF.** אל תנסה לגשת אליו ישירות או דרך פרוקסי;
  זה נבדק ונכשל. המידע מגיע מ-Gemini עם חיפוש מקורקע.

## בדיקה

`start.bat` מרים שרת ב-8899. לבדיקה בדפדפן, `window.pillApp` חושף את `S`, `UI`, `N`, `Sensors`, `Sch`
כדי לקרוא ל-`pillApp.UI.showView('track')` וכדומה בלי לצוד קואורדינטות.
